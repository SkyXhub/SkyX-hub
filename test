--[[
    🌊 SkyX Hub Demo Script 🌊
    
    This script demonstrates a simplified UI that focuses on making buttons and tabs visible.
    
    It uses no external dependencies or icon loading to ensure everything appears correctly.
]]

-- Load the basic UI library
local SkyXBasic = loadstring(game:HttpGet(""https://raw.githubusercontent.com/SkyXhub/SkyX-hub/refs/heads/main/SkyX%20UI%20Library.lua"))()

-- Create the main window
local Window = SkyXBasic:CreateWindow("SkyX Hub - Basic Demo")

-- Show initial notification
Window:Notify("Basic UI Loaded", "This is a simplified version designed to ensure buttons and tabs appear correctly", 5)

-- First tab - Main
local MainTab = Window:CreateTab("Main")

-- Sections
local InfoSection = MainTab:CreateSection("Information")
InfoSection:AddLabel("Welcome to SkyX Hub Basic")
InfoSection:AddLabel("Version: 1.0.0")
InfoSection:AddLabel("This UI uses direct assets - no GitHub loading")

local FeaturesSection = MainTab:CreateSection("Features")
FeaturesSection:AddButton("Example Button", function()
    Window:Notify("Button Clicked", "This button works!", 3)
end)

local TestToggle = FeaturesSection:AddToggle("Example Toggle", false, function(value)
    Window:Notify("Toggle Changed", "Toggle is now: " .. (value and "ON" or "OFF"), 3)
end)

-- Second tab - Settings
local SettingsTab = Window:CreateTab("Settings")

local AppearanceSection = SettingsTab:CreateSection("Appearance")
AppearanceSection:AddButton("Test Visibility", function()
    Window:Notify("UI Test", "If you can see this notification, the UI is working correctly!", 3)
end)

-- Third tab - Credits
local CreditsTab = Window:CreateTab("Credits")

local CreditsSection = CreditsTab:CreateSection("SkyX Team")
CreditsSection:AddLabel("Created by: SkyX Team")
CreditsSection:AddLabel("Thanks for using SkyX Hub!")

-- Add exit button
CreditsSection:AddButton("Exit", function()
    SkyXBasic:Destroy()
end)

-- Return UI to enable script hub functionality
return Window
